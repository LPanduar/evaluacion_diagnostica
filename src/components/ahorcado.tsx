"use client"

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function Ahorcado() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Juego del Ahorcado</CardTitle>
        <CardDescription>No realizado</CardDescription> {/* :3 */}
      </CardHeader>
    </Card>
  )
}

